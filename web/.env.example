# Production default: portfolio site with preview-only lab UI (no FastAPI required)
NEXT_PUBLIC_LAB_ENABLED=false

# Optional — only needed when NEXT_PUBLIC_LAB_ENABLED=true (local development)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8001
API_URL=http://127.0.0.1:8001
