FROM python:3.12-slim
WORKDIR /app/web/api
COPY web/api/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY web/api/ .
ENV HARMONIC_BIN=/app/harmonic_series
EXPOSE 8001
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8001"]
